---
description: Select the smallest safe reusable module set for this project
argument-hint: [the capability you need]
---

Load the `apply-reusable-modules` skill and follow it.

The capability the owner is looking for: $ARGUMENTS

Choose from `library/MODULE_CATALOG.md` and propose only `READY` modules,
smallest sufficient set first. Do not copy any module into the target
project as part of this step: selection is a proposal, and adopting a
module is the owner's decision.

If no catalogued module fits, say so plainly rather than stretching one to
fit.
